{
  "name": "custom_setup",
  "version": "1.0.0",
  "description": "custom setup pulumi-repl example",
  "main": "bin/index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node ./bin/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pulumi/random": "^4.0.0"
  }
}
